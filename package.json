{
  "name": "agentconfig",
  "version": "0.1.0",
  "private": true,
  "description": "AgentConfig Bundle — a portable, encrypted format for sharing AI agent configurations across desktop clients.",
  "license": "MIT",
  "author": "Zhuah Dong",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspace @agentconfig/core && npm run build --workspace @agentconfig/cli",
    "typecheck": "npm run typecheck --workspace @agentconfig/core && npm run typecheck --workspace @agentconfig/cli",
    "test": "vitest run",
    "site:check": "node scripts/check-site.mjs",
    "check": "npm run build && npm run typecheck && npm test && npm run site:check",
    "cli": "node packages/cli/dist/index.js"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "tsup": "^8.3.0"
  },
  "engines": { "node": ">=18.0.0" }
}
